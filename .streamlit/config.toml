[server]
# Maximum upload size in MB.
# XTC trajectory files routinely exceed 1 GB for production runs.
# TPR files are tiny (1–3 MB); that limit is enforced in code, not here.
maxUploadSize = 4096        # 4 GB ceiling — enough for most production XTC files

[browser]
gatherUsageStats = false
